# Tinker API key (https://tinker-console.thinkingmachines.ai)
TINKER_API_KEY=
# Secret passkey that defines the red/black token split. Treat like a signing key.
WM_PASSKEY=
# Optional: set if your fidelity embedding model should run on a GPU
# WM_EMBED_DEVICE=cuda
